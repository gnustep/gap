{
    "## Comment" = "Do NOT change this file, Gorm maintains it";
    AppController = {
	Actions = (
	    "showPrefPanel:",
	    "addFiles:",
	    "setScaleToFit:",
	    "setFullScreen:",
	    "nextImage:",
	    "prevImage:"
	);
	Outlets = (
	    controlWin,
	    fileListData,
	    fitButton,
	    fileListView,
	    scrollView,
	    view,
	    window,
	    fullScreenButton,
	    fullScreenMenuItem
	);
	Super = NSObject;
    };
    FileTable = {
	Actions = (
	);
	Outlets = (
	);
	Super = NSObject;
    };
    FirstResponder = {
	Actions = (
	    "addFiles:",
	    "setFullScreen:",
	    "nextImage:",
	    "orderFrontFontPanel:",
	    "prevImage:",
	    "setScaleToFit:",
	    "showPrefPanel:"
	);
	Super = NSObject;
    };
}