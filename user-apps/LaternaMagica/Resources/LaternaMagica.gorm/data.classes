{
    "## Comment" = "Do NOT change this file, Gorm maintains it";
    AppController = {
	Actions = (
	    "showPrefPanel:",
	    "addFiles:",
	    "setScaleToFit:",
	    "setFullScreen:",
	    "nextImage:",
	    "prevImage:",
	    "removeImage:",
	    "eraseImage:"
	);
	Outlets = (
	    controlWin,
	    fileListData,
	    fitButton,
	    fileListView,
	    scrollView,
	    smallView,
	    smallWindow,
	    fullScreenButton,
	    fullScreenMenuItem
	);
	Super = NSObject;
    };
    FileTable = {
	Actions = (
	);
	Outlets = (
	);
	Super = NSObject;
    };
    FirstResponder = {
	Actions = (
	    "addFiles:",
	    "eraseImage:",
	    "nextImage:",
	    "orderFrontFontPanel:",
	    "prevImage:",
	    "removeImage:",
	    "setFullScreen:",
	    "setScaleToFit:",
	    "showPrefPanel:"
	);
	Super = NSObject;
    };
    LMFlipView = {
	Actions = (
	);
	Outlets = (
	);
	Super = NSImageView;
    };
}