{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {
                fllButtPressed = id; 
                makeKeyAndOrderFront = id; 
                setLnCap = id; 
                setLnJoin = id; 
                stepperAction = id; 
                stkButtPressed = id; 
                valuesChanged = id; 
            }; 
            CLASS = GRPropsEditor; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                fillColorWell = NSColorWell; 
                flatnessField = NSTextField; 
                flatnessStepper = NSStepper; 
                fllButt = NSButton; 
                lineCapMatrix = NSMatrix; 
                lineJoinMatrix = NSMatrix; 
                linewidthField = NSTextField; 
                linewidthStepper = NSStepper; 
                miterlimitField = NSTextField; 
                miterlimitStepper = NSStepper; 
                propsPanel = NSPanel; 
                stkButt = NSButton; 
                strokeColorWell = NSColorWell; 
            }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}