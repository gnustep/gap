{
    "## Comment" = "Do NOT change this file, Gorm maintains it";
    DBObjectInspector = {
	Actions = (
	    "search:",
	    "loadObject:",
	    "updateObject:"
	);
	Outlets = (
	    fieldObjId,
	    winObjInspector,
	    fieldTable,
	    faultTextView,
	    faultPanel,
	    updateButton,
	    searchField,
	    statusField
	);
	Super = NSObject;
    };
    FirstResponder = {
	Actions = (
	    "loadObject:",
	    "search:",
	    "updateObject:"
	);
	Super = NSObject;
    };
}