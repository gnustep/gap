{
    IBClasses = (
        {
            ACTIONS = {loadObject = id; updateObject = id; }; 
            CLASS = DBObjectInspector; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                faultPanel = NSPanel; 
                faultTextView = NSTextView; 
                fieldObjId = NSTextField; 
                fieldTable = NSTableView; 
                updateButton = NSButton; 
                winObjInspector = NSWindow; 
            }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}