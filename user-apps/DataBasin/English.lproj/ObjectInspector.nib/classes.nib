{
    IBClasses = (
        {
            ACTIONS = {loadObject = id; search = id; updateObject = id; }; 
            CLASS = DBObjectInspector; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                faultPanel = NSPanel; 
                faultTextView = NSTextView; 
                fieldObjId = NSTextField; 
                fieldTable = NSTableView; 
                searchField = NSSearchField; 
                statusField = NSTextField; 
                updateButton = NSButton; 
                winObjInspector = NSWindow; 
            }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}