{
    IBClasses = (
        {
            ACTIONS = {
                anonymousConn = id; 
                cancelConn = id; 
                changePathFromMenu = id; 
                connectConn = id; 
                listDoubleClick = id; 
                showConnPanel = id; 
                showFtpLog = id; 
                showPrefPanel = id; 
            }; 
            CLASS = AppController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                connAddress = NSTextField; 
                connAnon = NSButton; 
                connPass = NSTextField; 
                connPort = NSTextField; 
                connUser = NSTextField; 
                connectPanel = NSPanel; 
                localPath = NSPopUpButton; 
                localView = NSTableView; 
                logTextField = NSTextView; 
                logWin = NSWindow; 
                remotePath = NSPopUpButton; 
                remoteView = NSTableView; 
            }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}