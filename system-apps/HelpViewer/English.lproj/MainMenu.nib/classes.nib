{
    IBClasses = (
        {
            ACTIONS = {
                back = id; 
                bookshelf = id; 
                index = id; 
                openFile = id; 
                print = id; 
                search = id; 
            }; 
            CLASS = Controller; 
            LANGUAGE = ObjC; 
            OUTLETS = {helpMenu = id; infoMenu = id; servicesMenu = id; }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}