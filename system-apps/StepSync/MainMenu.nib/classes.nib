{
    IBClasses = (
        {
            ACTIONS = {
                analyzeAction = id; 
                applyPreferences = id; 
                setSourcePath = id; 
                setTargetPath = id; 
                showPreferences = id; 
                stopTask = id; 
                syncAction = id; 
            }; 
            CLASS = AppController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                analyzeButton = NSButton; 
                deleteItemsCheck = NSButton; 
                handleDirectoriesCheck = NSButton; 
                insertItemsCheck = NSButton; 
                logView = NSTextView; 
                prefPanel = NSPanel; 
                progressBar = NSProgressIndicator; 
                skipHiddenFilesCheck = NSButton; 
                skipHiddenFoldersCheck = NSButton; 
                skipThumbFilesCheck = NSButton; 
                sourceDirNumberField = NSTextField; 
                sourceFileNumberField = NSTextField; 
                sourcePathField = NSTextField; 
                sourceSizeField = NSTextField; 
                stopButton = NSButton; 
                syncButton = NSButton; 
                targetDirNumberField = NSTextField; 
                targetFileNumberField = NSTextField; 
                targetPathField = NSTextField; 
                targetSizeField = NSTextField; 
                updateItemsCheck = NSButton; 
                updateSourceCheck = NSButton; 
            }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}